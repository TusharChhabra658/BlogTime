<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("./partials/head") %>
    <title><%= blog.title %></title>
  </head>
  <body>
    <%- include("./partials/nav")%>
    <div class="container-fluid p-4">
        <div class="row justify-content-center">
          <div class="col-12 col-md-10 col-lg-8">
            <h1 class="display-4 fw-bold mb-3"><%= blog.title %></h1>
            <img src="<%= blog.coverImageUrl %>" alt="Blog Image" class="img-fluid rounded mb-4" width="1000rem" height="500vh">
            <div class="blog-body fs-5 lh-lg mb-4">
              <p><%= blog.body %></p>
            </div>
            <div class="container fs-6">
                <p>Written by :</p>
              <img src="<%= blog.createdBy.profileImageUrl %>" alt="" width="40rem">
              <%= blog.createdBy.fullName %>
            </div>
          </div>
        </div>
      </div>
    <%- include("./partials/scripts") %>
  </body>
</html>
